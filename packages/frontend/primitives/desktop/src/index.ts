/**
 * @gouide/primitives-desktop
 * Desktop platform primitives
 *
 * For Tauri (WebView-based), we use the same DOM primitives as web.
 * This re-export ensures a consistent interface while allowing for
 * future platform-specific optimizations if needed.
 */

export * from '@gouide/primitives-web';
